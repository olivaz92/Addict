{% extends 'base.html.twig' %}

{% block title %}MessageAdmin{% endblock %}

{% block body %}
    <h1>MessageAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message_admin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ message_admin.nom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ message_admin.telephone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ message_admin.mail }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message_admin.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_admin_index') }}">back to list</a>

    <a href="{{ path('message_admin_edit', {'id': message_admin.id}) }}">edit</a>

    {{ include('message_admin/_delete_form.html.twig') }}
{% endblock %}
