{% extends 'base.html.twig' %}

{% block title %}Pense{% endblock %}

{% block body %}
    <h1>Pense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pense.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ pense.libelle }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pense.date ? pense.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pense_index') }}">back to list</a>

    <a href="{{ path('pense_edit', {'id': pense.id}) }}">edit</a>

    {{ include('pense/_delete_form.html.twig') }}
{% endblock %}
